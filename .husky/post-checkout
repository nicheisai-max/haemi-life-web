#!/bin/sh

# Master Guard Dispatcher (Branch, Schema, Identity, Code Quality, Size)
npx tsx scripts/branch_guard.ts --commit || exit 1

# Forensic Hygiene Sync
npx tsx scripts/repo-hygiene.ts --silent || echo "Hygiene skip."
