#!/bin/sh

# HAEMI LIFE — HUSKY COMMIT-MSG HOOK
# Master Governance Validation

# Redirect to Master Guard if commit message validation is extended there
node .github/governance/commit-msg-guard.js "$1" || exit 1
